<div class="header">
    <%= render template: "templates/_profile_icon" %>
    <div class="inner-header" style="margin-top: 0px !important; margin-bottom: 100px;">
        <h1>Личный кабинет</h1>
        <p class="appointment-form-label">Ваш Email: <%= current_doctor.email %></p>
        <p class="appointment-form-label">Ваш телефон: <%= current_doctor.phone_number %></p>
        <a href="/doctors/edit" class="table-link">Редактировать профиль</a>
        <a href="/doctors/sign_out" class="table-link">Выйти из уч. записи</a>

        <h1>Ваши консультации</h1>
         <table class="appointments-table" style="margin-top: 50px;">
            <tr>
                <th>Дата</th>
                <th>Пациент</th>
                <th>Статус</th>
            </tr>
            <% @appointments.each do |appointment| %>
              <% if appointment.user %>
                <tr>
                    <td>
                        <a href="/appointments/<%= appointment.id %>/new_recomendation" class="table-link"><%= appointment.desired_time %></a>
                    </td>
                    <td>
                        <a href="/appointments/<%= appointment.id %>/new_recomendation" class="table-link"><%= appointment.user.name %></a>
                    </td>
                    <td>
                    <a href="/appointments/<%= appointment.id %>/new_recomendation" class="table-link">
                        <% if appointment.active %>
                          Предстоит
                        <% else %> 
                          Состоялась   
                        <% end %>
                    </a>   
                    </td>
                </tr>
              <% end %>
            <% end %>
        </table>

       
    </div>
    <%= render template: "templates/_waves" %>
</div>